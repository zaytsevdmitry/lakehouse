server:
  port: 0

spring:
  main:
    web-application-type: none
logging:
  structured:
    format: none
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
  level:
    root: info
