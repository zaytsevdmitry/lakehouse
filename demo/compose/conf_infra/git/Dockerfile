# Lightweight git server for the CVS/GitOps demo: alpine/git image with the
# git-daemon subpackage (Alpine ships `git daemon` separately from the git CLI).
FROM alpine/git:latest
RUN apk add --no-cache git-daemon