kind: Task
name: sparkTemplate
taskExecutionServiceGroupName: spark-cluster
taskProcessor: sparkStandAloneClusterTaskProcessor
importance: critical
description: load from remote datastore
driverKeyName: spark_iceberg
taskProcessorArgs: 
  spark.kubernetes.driver.pod.name: "task-{{ id }}-{{tryNum}}-{{name}}-driver"
  spark.app.name: "{{ id }}-{{tryNum}}-{{name}}"
  spark.kubernetes.submission.waitAppCompletion: false
  spark.kubernetes.executor.deleteOnTermination: true
  spark.kubernetes.driver.annotation.lakehouse-management-task: "{{ getTaskFullName() }}"
  deploy.appResource: "/opt/lakehouse-task-spark-apps/lakehouse-task-executor-spark-dataset-app-0.11.0-jar-with-dependencies.jar"
  deploy.clusterUrl: "http://172.20.193.160:8086/v1/submissions"
  deploy.mainClass: org.lakehouse.taskexecutor.spark.dataset.SparkProcessorApplication
  spark.driver.cores: 1
  spark.driver.memory: 1g
  spark.executor.cores: 1
  spark.executor.memory: 1g
  spark.ui.enabled: true