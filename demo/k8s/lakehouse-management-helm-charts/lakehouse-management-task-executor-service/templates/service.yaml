apiVersion: v1
kind: Service
metadata:
  name: lakehouse-management-task-executor-service
spec:
  type: NodePort
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
  selector:
    app.kubernetes.io/name: lakehouse-management-task-executor-service
    app.kubernetes.io/instance: {{ .Release.Name }}
