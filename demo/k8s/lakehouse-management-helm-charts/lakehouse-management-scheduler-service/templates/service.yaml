apiVersion: v1
kind: Service
metadata:
  name: lakehouse-management-scheduler-service
spec:
  type: NodePort # Делаем сервис доступным извне через порт
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    app.kubernetes.io/name: lakehouse-management-scheduler-service
    app.kubernetes.io/instance: {{ .Release.Name }}
