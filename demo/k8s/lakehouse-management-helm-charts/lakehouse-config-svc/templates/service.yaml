apiVersion: v1
kind: Service
metadata:
  name: lakehouse-config-svc
spec:
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app.kubernetes.io/name: lakehouse-config-svc
    app.kubernetes.io/instance: {{ .Release.Name }}
