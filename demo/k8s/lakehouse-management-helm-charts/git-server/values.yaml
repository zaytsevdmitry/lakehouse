image:
  repository: alpine/git
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9418

persistence:
  enabled: false
  # existingClaim: "git-server-pvc"

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
