apiVersion: v1
kind: Service
metadata:
  name: spark-history
spec:
  type: ClusterIP
  ports:
    - port: 18080
      targetPort: 18080
      protocol: TCP
      name: http
  selector:
    app: spark-history

