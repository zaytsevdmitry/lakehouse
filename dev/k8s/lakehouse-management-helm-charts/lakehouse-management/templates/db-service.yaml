apiVersion: v1
kind: Service
metadata:
  name: db-dev # Это имя станет DNS-именем внутри кластера
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-dev
