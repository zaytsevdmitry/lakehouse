apiVersion: v1
kind: Service
metadata:
  name: broker # Это имя станет хостом в K8s
spec:
  type: ClusterIP
  ports:
    - name: client
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093
  selector:
    app: broker
